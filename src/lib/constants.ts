/** Maximum CSV file size in bytes (10 MB) */
export const MAX_CSV_FILE_SIZE = 10 * 1024 * 1024;

/** Minimum transaction amount for recurring detection (DKK) */
export const RECURRING_MIN_AMOUNT = 50;

/** Default number of months to look back for forecasting */
export const FORECAST_DEFAULT_LOOKBACK = 3;

/** Timeout for AI categorization requests (ms) */
export const AI_CATEGORIZE_TIMEOUT = 60_000;
